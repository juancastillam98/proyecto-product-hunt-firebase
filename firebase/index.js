//este fichero va a ser la unión entre context t firebase
import {firebase} from  "./firebase" //el firebase.js
import {FirebaseContext} from "../firebase/context"

export {FirebaseContext}
export default firebase;